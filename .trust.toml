schema_version = 1
profile = "standard"

[lifecycle]
command = ["fledge", "lanes", "run", "verify"]

[contract]
enabled = false
require_coverage = 0
skip_reason = "Repo has no SpecSync specs yet and a full contract backfill is out of scope for this pin"

[risk]
threshold = "block"

[provenance]
mode = "soft"
policy = ".attest.json"
skip_reason = ""

[atlas]
enabled = false
skip_reason = "Repo has no Atlas workflow and publication is not wanted here"
